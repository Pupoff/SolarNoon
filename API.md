# API reference

TODO — exhaustive reference for every user-facing function, struct, and
constant, organized for lookup rather than reading top to bottom.

Until this is written, see [GETTING_STARTED.md](GETTING_STARTED.md) for the
narrative explanation of the main building blocks, and the headers themselves
for exact signatures: `leds_rgb.h`, `leds_mono.h`, `midi.h`, `knobs.h`,
`switches.h`, `effects.h`, `display.h`.
